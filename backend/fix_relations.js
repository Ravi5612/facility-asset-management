const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const employees = await prisma.employee.findMany(); let count = 0; for (const emp of employees) { if (emp.email) { const result = await prisma.user.updateMany({ where: { email: emp.email }, data: { employeeId: emp.id } }); count += result.count; } } console.log('Updated ' + count + ' users'); } main().finally(() => prisma.$disconnect());
